import 'package:flutter/material.dart';

class SliverListPinningDemo extends StatelessWidget {
  const SliverListPinningDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}